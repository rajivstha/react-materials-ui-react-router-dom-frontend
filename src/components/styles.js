import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
    headerContainer: {
      flexGrow: 1,
    },
    headerTitle: {
      flexGrow: 1,
    },
    footerContainer: {
        backgroundColor: "#3f51b5",
        color: "#fff",
        textAlign: "center",
        padding: 15
    }
}));